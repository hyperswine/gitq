use arcen::common::Component
use arcen::components::Box,Heading,Text,Flex

export fn NavBar() -> Component {
    @arcen {
        Box {
            Heading { "Git Q" }
            Flex {
                
            }
        }
    }
}
